import { UperdollarPipe } from './uperdollar.pipe';

describe('UperdollarPipe', () => {
  it('create an instance', () => {
    const pipe = new UperdollarPipe();
    expect(pipe).toBeTruthy();
  });
});
